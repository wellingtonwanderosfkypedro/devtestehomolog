import { ButtonLink, ImageData } from "@/helpers/generalTypes";

export interface ManualLGPDTypes {
  tag: string;
  title: string;
  text: string;
  button: ButtonLink;
  image: ImageData;
  imageMob: ImageData;
}
